# 🔑 환경변수 설정 템플릿
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값을 입력하세요

# ================================
# OpenAI API 설정
# ================================
# OpenAI API 키 (필수)
# https://platform.openai.com/api-keys 에서 발급받으세요
OPENAI_API_KEY=your_openai_api_key_here

# ================================
# 로컬 릴레이 서버 설정 (선택사항)
# ================================
# 로컬 릴레이 서버 URL
# 릴레이 서버를 사용하면 API 키를 숨기고 서버에서 커스텀 로직 실행 가능
# 사용하지 않으려면 주석 처리하거나 빈 값으로 설정
REACT_APP_LOCAL_RELAY_SERVER_URL=http://localhost:8081

# ================================
# 개발 환경 설정
# ================================
# React 앱 포트 (기본값: 3000)
PORT=3000

# 개발 모드에서 브라우저 자동 열기 (true/false)
BROWSER=true

# Fast Refresh 활성화 (true/false)
FAST_REFRESH=true

# ================================
# 프로덕션 환경 설정
# ================================
# 빌드 경로 (기본값: build)
BUILD_PATH=build

# Public URL (배포시 사용)
# PUBLIC_URL=https://your-domain.com

# ================================
# 디버깅 및 로깅
# ================================
# 디버그 모드 (development/production)
NODE_ENV=development

# 상세 로그 출력 (true/false)
REACT_APP_DEBUG=false

# ================================
# 보안 설정
# ================================
# HTTPS 사용 (개발 환경에서도 HTTPS 사용하려면 true)
HTTPS=false

# SSL 인증서 파일 경로 (HTTPS=true일 때 사용)
# SSL_CRT_FILE=
# SSL_KEY_FILE=

# ================================
# 기타 설정
# ================================
# 앱 버전
REACT_APP_VERSION=1.0.0

# 앱 이름
REACT_APP_NAME="Catch Up AI - Vibe Coding"

# 지원 이메일
REACT_APP_SUPPORT_EMAIL=support@catchupai.com

# ================================
# 사용 방법
# ================================
# 1. 이 파일을 .env로 복사: copy .env.example .env
# 2. 실제 값들로 변경
# 3. OpenAI API 키는 반드시 설정 필요
# 4. .env 파일은 절대 Git에 커밋하지 마세요!

# ================================
# 주의사항
# ================================
# ⚠️ API 키나 비밀번호 등 민감한 정보는 절대 공개하지 마세요
# ⚠️ .env 파일은 .gitignore에 포함되어 있어 Git에 추적되지 않습니다
# ⚠️ 환경변수 변경 후에는 개발 서버를 재시작해야 합니다
# ⚠️ REACT_APP_ 접두사가 있는 변수만 클라이언트에서 접근 가능합니다
